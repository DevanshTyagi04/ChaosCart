apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: user-service

spec:
  selector:
    app: user-service

  ports:
    - name: http
      port: 4001
      targetPort: 4001

---
apiVersion: apps/v1
kind: Deployment

metadata:
  name: user-service
  namespace: {{ .Release.Namespace }}

spec:
  replicas: {{ .Values.userService.replicaCount }}

  selector:
    matchLabels:
      app: user-service

  template:
    metadata:
      labels:
        app: user-service


    spec:
      containers:
      - name: user-service

        image: "{{ .Values.userService.image.repository }}:{{ .Values.userService.image.tag }}"

        imagePullPolicy: Always

        env:
          - name: DB_HOST
            value: {{ .Values.userService.database.host | quote }}

          - name: DB_PORT
            value: {{ .Values.userService.database.port | quote }}

          - name: DB_NAME
            value: {{ .Values.userService.database.name | quote }}

          - name: DB_USER
            valueFrom:
              secretKeyRef:
                name: postgres-secret
                key: POSTGRES_USER

          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: postgres-secret
                key: POSTGRES_PASSWORD

        ports:
          - containerPort: 4001

        readinessProbe:
          httpGet:
            path: /health
            port: 4001
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        livenessProbe:
          httpGet:
            path: /health
            port: 4001
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3

        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"

          limits:
            cpu: "500m"
            memory: "512Mi"

